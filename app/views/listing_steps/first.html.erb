<%= form_for @listing, url: wizard_path do |f| %>
  <% if @listing.errors.any? %>
    <div id="error_explanation" class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@listing.errors.count, "error") %> prohibited this listing from being saved:</h4>

      <ul>
      <% @listing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :state, "Estado" %><br>
    <%= f.select(:state, ['Used', 'New'], {include_blank: "Select something"}, class: "form-control") %>
  </div>

  <div class="form-group">
    <%= f.label :macro_category, "Macro descripción equipo" %><br>
    <%= f.select(:macro_category, ['Tuberias', 'Equipamiento Pozos', 'Equipos Superficie'], {include_blank: "Select something"}, class: "form-control") %>
  </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-info"%>
  </div>
<% end %>